---
title: "Gestión visual de datos para comunicación científica en los distintos ámbitos académicos"
subtitle: "Programa de Formación Transversal de Doctorado \n Universidad de Oviedo"  
author: 
  - "Noelia Rico y Raúl Pérez-Fernández"
date: '7-8 MAYO 2021'
output:
  xaringan::moon_reader:
    lib_dir: libs
    css: xaringan-themer.css
    nature:
      highlightStyle: github
      highlightLines: true
      countIncrementalSlides: false
---

```{r setup, include=FALSE}
library(tidyverse)
library(palmerpenguins)
library(babynames)
library(gapminder)
library(gganimate)
library(plotly)
library(gifski)
options(htmltools.dir.version = FALSE)
options(htmltools.preserve.raw = FALSE)
knitr::opts_chunk$set(fig.retina = 3, fig.align = 'center', warning = FALSE, message = FALSE)
```



```{r xaringan-themer, include=FALSE, warning=FALSE}

library(xaringanthemer)
style_mono_accent(
  base_color = "#1c5253",
  header_font_google = google_font("Josefin Sans"),
  text_font_google   = google_font("Montserrat", "300", "300i"),
  code_font_google   = google_font("Fira Mono")
)
```


# Objetivos

1. Manejar R desde su IDE, RStudio, para el uso del paquete `ggplot`
1. Conocer la filosofía **Grammar of graphics**
1. Enfoque práctico

---


name: gog
# Grammar of graphics

- Aproximación teórica sobre como construir gráficos https://www.springer.com/gp/book/9780387245447
- Muchos programas: ggplot, tableau, vega-lite
- No se centra en
  - el tipo de gráfico que es mejor en función del tipo de datos
  - el aspecto visual del gráfico
  - el algoritmo necesario para definir el gráfico
- Sí se centra en el mecanismo. ¿Cómo diseñar el sistema que nos permite construir el gráfico?



<!--
# Grammar of graphics

Descompone los gráficos en capas

- Tema
- Coordinadas
- Facets
- Geometrías
- Escalas
- Estadísticas
- Mapeo
- Datos
-->

---

# Grammar of graphics

- **Datos**: la estructura de los datos define qué se puede hacer con ellos. Se requiere un formato ordenado.
- **Mapeo**:
  - Estético: asociar variables de los datos con propiedades geométricas del gráfico.
  - En facetas: asociar variables con distintos paneles del gráfico.
- **Estadisticas**: aunque los datos están ordenados pueden no representar los valores que queremos mostrar, lo que puede motivar la necesidad de estadísticas adicionales. <!--Por ejemplo: contar cuantas observaciones de cada tipo tengo. Los summary statistics para un ggplot-->
- **Escalas:** traducen entre rangos de variables y propiedades de variables. por ejemplo categorias colores o valores numeros. <!--es la traducción de tal valor a tal color, la leyenda. colores escala gradual o discreta-->
- **Geometría**: determinación del tipo de gráfico.
- **Facetas**: dividen los datos en subconjuntos que se representan en paneles. <!--Además la distribución de estos sub los paneles puede aportar significado. muy utiles para plot driven data investigation-->
- **Coordinadas**: escalado para las *aesthetics* que son posicionales. Definen el canvas sobre el que se pintará el resto del gráfico.
- **Tema**: (*visual flourish*) lo único que no está relacionado con los datos. Permite modificar los aspectos visuales para hacerlos atractivos al visor. <!-- visual flourish. daltónicos. no importante para aprender-->

---

# `ggplot`

- Grammar of graphics es un sistema de diseño, `ggplot` es una implementación.
- Teoría y práctica son diferentes - algunas de las capas pueden mapearse juntas.

### Instalación y carga del paquete en R

```
install.packages("ggplot")
library(ggplot)
```

---

#  Datos: `penguins` 🐧 

```
install.packages("palmerpenguins")
palmerpenguins::penguins
```

Datos de 344 pingüinos de  3 especies diferentes, recogidos de 3 islas del Archipelago Palmer, Antárctica.

- `species           ` : Adelie, Chinstra, Gentoo. Tipo `fct` 
- `island            ` : Biscoe, Dream, Torgersen. Tipo `fct` 
- `bill_length_mm    ` : Longitud del pico. Tipo `dbl` 
- `bill_depth_mm     ` : Profundidad del pico. Tipo `dbl` 
- `flipper_length_mm ` : Longitud de la aleta. Tipo `int` 
- `body_mass_g       ` : Peso. Tipo `int` 
- `sex               ` : Sexo. Tipo `fct` 
- `year              ` : Año (2007, 2008, 2009). Tipo `int` 


---

# Pingüinos 🐧

```{r echo=FALSE, tidy=FALSE}
rmarkdown::paged_table(penguins)
```

---
class:center, middle

# Gráficos de dispersión
 
y además aesthetics + un poco de personalización

---

# Gráfico de dispersión

```{r echo=FALSE, fig.height=4}
ggplot(penguins, aes(x = bill_length_mm, y = bill_depth_mm)) +
  geom_point()
```

--

```
ggplot(penguins, aes(x = bill_length_mm, y = bill_depth_mm)) +
  geom_point()
```

--

O también:

```
*ggplot(penguins, aes(bill_length_mm, bill_depth_mm)) +
  geom_point()
```

---

## Color 🎨

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm)) +
  geom_point(color = "blue")
```

Colores por defecto disponibles: http://sape.inf.usi.ch/quick-reference/ggplot2/colour 

---

## Color personalizado 🧑‍🎨

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm)) +
  geom_point(color = "#fabada")
```

Un generador de paletas en colores: [https://coolors.com](https://coolors.com)

---

## Solapando `geom`s

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm)) +
  geom_point(color = "#fabada") +
  geom_hline(yintercept = 15.2) #<<
```

---

## Solapando `geom`s

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm)) +
  geom_point(color = "#fabada") +
  geom_hline(yintercept = 15.2) +
  geom_vline(xintercept = 43) #<<
```

---

## Solapando `geom`s

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm)) +
  geom_point(color = "#fabada") +
  geom_hline(yintercept = 15.2, color = "red", linetype = "dotted") +  #<<
  geom_vline(xintercept = 43, color = "grey", linetype = "dashed")  #<<
```

Puedes encontrar un resumen de todos los tipos de línea [aquí](http://www.sthda.com/english/wiki/ggplot2-line-types-how-to-change-line-types-of-a-graph-in-r-software).

---

## Rectas genéricas

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm)) +
  geom_point(color = "#fabada") +
  geom_abline(slope = .3, intercept = 2) #<<
```

---

## El tamaño importa ☝🏻

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm)) +
  geom_point(color = "#fabada") +
  geom_abline(slope = .3, intercept = 2, size = 2) #<<
```

---

## El orden de las capas también 🤭

--

```{r, fig.height=5}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm)) +
  geom_abline(slope = .3, intercept = 2, size = 2) + #<<
  geom_point(color = "#fabada") #<<
```

---

## Mapeando variables como `color`

```{r, fig.height=5}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm, color = sex)) + #<<
  geom_point()
```

---

## Aesthetic vs. apariencia ⚠️


```{r, fig.height=2}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm)) +
  geom_point(color = "blue") #<<
```


```{r, fig.height=2}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm, color = sex)) + #<<
  geom_point()
```


---

## Personalizando la escala de colores

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm, color = sex)) +
  geom_point() +
  scale_color_manual(values = c("green", "orange")) #<<
```

[Aquí](http://www.sthda.com/english/wiki/ggplot2-colors-how-to-change-colors-automatically-and-manually) puedes encontrar más ejemplo, así como paletas de colores ya predefinidas en R.


---

## Utilizando condiciones para colorear

```{r, fig.height=5}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm, 
                     color = body_mass_g > 4000)) + #<<
  geom_point()
```


---

## Más aesthetics: shape

```{r echo=FALSE, fig.height=4}
ggpubr::show_point_shapes()
```

Por defecto, la shape utilizada es la número 16.

- Algunas tienen solo `color`, que rellena el interior de la forma.
- Otras tienen `color` para el borde y `fill` para su interior.

---

## Más aesthetics: shape

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm)) +
  geom_point(shape = 17) #<<
```

---

## De nuevo, aesthetic vs. apariencia ⚠️

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm, 
                     shape = island)) + #<<
  geom_point()
```
---

## También podemos combinarlas

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm, 
                     shape = island, color = sex)) + #<<
  geom_point()
```

---

## Diferencia entre color y fill

Vamos a elegir una de las formas con contorno:

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm)) +
  geom_point(shape = 23)
```

---

## Diferencia entre color y fill

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm, 
                     color = sex)) + #<<
  geom_point(shape = 23)
```

---

## Diferencia entre color y fill

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm, 
                     fill = sex)) + #<<
  geom_point(shape = 23)
```

---

## Diferencia entre color y fill

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm, 
                     color = island, fill = sex)) + #<<
  geom_point(shape = 23)
```

Poder, se puede pero... 🥴

---

## Otra aesthetic: `size`

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm, size = island)) +
  geom_point(color = "blue")
```

---

## El valor de `alpha`

Habitualmente cuando trabajamos con `size` los puntos se solapan

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm, size = island)) +
  geom_point(color = "blue", alpha = .4)
```

---

## La aesthetic `alpha`

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm, 
                     size = island,
                     alpha = island)) + #<<
  geom_point(color = "blue")
```

Observa cómo se unifican las leyendas al mapear la misma variable en dos aesthetics.

--- 

## `size` para valores continuos

```{r, fig.height=5}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm, size = year)) +
  geom_point(color = "blue")
```

---

## Forzar una variable a ser discreta

```{r, fig.height=5}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm, size = as.factor(year))) +
  geom_point(color = "blue")
```

---

## Modificar el título de la leyenda

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm, size = as.factor(year))) +
  geom_point(color = "blue") +
  labs(size = "Año")
```

---

## Variable discreta en el eje x

Hasta ahora hemos trabajado con variables continuas en ambos ejes.

--

```{r, fig.height=4}
ggplot(penguins, aes(species, body_mass_g)) +
  geom_point()
```

---

## Color en variable continua

Hasta ahora hemos trabajado con variables discretas para el color.

```{r, fig.height=4}
ggplot(penguins, aes(species, body_mass_g, color = body_mass_g)) +
  geom_point()
```

--

Para shape también hemos utilizado solo variables discretas, ¿qué pasa si intentamos poner una continua?

---

## Color con variable discreta

Ya conocemos esta forma de colorear utilizando una variable discreta:

```{r, fig.height=4}
ggplot(penguins, aes(species, body_mass_g, color = species)) +
  geom_point()
```

--

En esta represetación los puntos quedan muy solapados, podemos añadir algo de aleatoreidad en el eje $x$ para cambiar la representación.
---

## Añadiendo jitter

```{r, fig.height=4}
ggplot(penguins, aes(species, body_mass_g, color = species)) +
  geom_jitter(height = 0)
```

---

## Estadística

```{r, fig.height=4}
ggplot(penguins, aes(species, body_mass_g, color = species)) +
  geom_jitter() +
  stat_summary(fun = mean)
```

---

## Aesthetics exclusivas de cada geom

```{r, fig.height=4}
ggplot(penguins, aes(species, body_mass_g)) +
  geom_point(aes(color = species)) +
  stat_summary(fun = mean)
```


---

## Aesthetics exclusivas de cada geom

```{r, fig.height=4}
ggplot(penguins, aes(species, body_mass_g)) +
  stat_summary(fun = mean, size = 2) +
  geom_point(aes(color = species)) 


```

---

# Comparación 

Ambas representaciones producen la misma salida:

```
ggplot(penguins, aes(species, body_mass_g, color = species)) +
  geom_point() 
```

```
ggplot(penguins, aes(species, body_mass_g)) +
  geom_point(aes(color = species)) 
```

Esta produce una salida diferente

```
ggplot(penguins, aes(species, body_mass_g)) +
  geom_point(color = "blue") 
```

---

## Dos variables discretas

¿Qué pasa si queremos hacer un diagrama similar con dos variables discretas?

--

```{r, fig.height=4}
ggplot(penguins, aes(island, species, color = species)) +
  geom_point()
```


---

## Utilizando jitter

```{r, fig.height=4}
ggplot(penguins, aes(island, species, color = species)) +
  geom_jitter()
```

En este caso podemos permitir aleatoriedad en ambos ejes

---

# Modificando las etiquetas

```{r, fig.height=4}
ggplot(penguins, aes(island, species, color = species)) +
  geom_jitter() +
  xlab("Especie") +
  ylab("Isla") 
```

---

# Modificando las etiquetas

Otra alternativa:

```{r, fig.height=4}
ggplot(penguins, aes(island, species, color = species)) +
  geom_jitter() +
  xlab("Especie") +
  ylab("Isla") 
```

---

## Almacenando un objeto

```{r, fig.height=4}
p <- ggplot(penguins, aes(bill_length_mm, bill_depth_mm)) +
  geom_point(color = "blue")
p
```

Ahora el objeto `p` está disponible para hacer cualquier tipo de modificación.

---

```{r}
p + labs(title="Relación bill_depth/bill_length en mm")
```

---

# Temas

El paquete tiene temas predefinidos que permiten modificaciones rápidas del aspecto visual.

- `theme_grey`
- `theme_bw`
- `theme_linedraw`
- `theme_light`
- `theme_dark`
- `theme_minimal`
- `theme_classic`
- `theme_void`

---

.pull-left[

```{r, fig.height=4}
p + theme_grey()
```

```{r, fig.height=4}
p + theme_linedraw()
```


]

.pull-right[

```{r, fig.height=4}
p + theme_bw()
```

```{r, fig.height=4}
p + theme_light()
```

]

---


.pull-left[

```{r, fig.height=4}
p + theme_dark()
```

```{r, fig.height=4}
p + theme_classic()
```


]

.pull-right[

```{r, fig.height=4}
p + theme_minimal()
```

```{r, fig.height=4}
p + theme_void()
```

]

Puedes encontrar más información sobre los temas [aquí](https://ggplot2.tidyverse.org/reference/ggtheme.html).

---

# Facets

Nos permiten visualizar subconjuntos de los datos.

```{r}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm)) +
  geom_point() +
  facet_wrap(.~species)
```

---

---

# Facets: change the layout

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm)) +
  geom_point() +
  facet_wrap(.~species, nrow = 3)
```

---


---

# Facets: free scales

```{r}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm)) +
  geom_point() + 
  facet_wrap(~species, scales = "free")
```

---

# Facets for two variables

```{r, fig.height=4}
ggplot(penguins, aes(bill_length_mm, bill_depth_mm)) +
  geom_point() + 
  facet_grid(island~species, scales = "free") #<<
```

---

## Histogramas

```{r, fig.height=4}
ggplot(penguins, aes(body_mass_g)) +
  geom_histogram(binwidth = 1000, 
                 color = "black",
                 fill = "white")
```

---

## Gráficos de barras

```{r, fig.height=4}
ggplot(penguins, aes(sex, fill = island)) +
  geom_bar(color = "black")
```

---

## Barras con datos ya sumarizados

```{r, fig.height=4}
datos_nuevos <- penguins %>% group_by(sex) %>% count()

ggplot(datos_nuevos, aes(sex, n)) +
  geom_bar(stat = "identity")
```

---

# Cambio de coordenadas

```{r, fig.height=4}
ggplot(penguins, aes(sex)) +
  geom_bar() +
  coord_flip()
```

---

## Eliminar niveles 

```{r, fig.height=4}
ggplot(penguins, aes(sex)) +
  geom_bar() +
  scale_x_discrete(limits = c("female", "male"))
```

---

## Reordenar los niveles e introducir estética

```{r, fig.height=4}
ggplot(penguins, aes(sex)) +
  geom_bar(fill = "lightblue", color = "black") +
  scale_x_discrete(limits = c("male", "female")) +
  geom_text(stat = "count", aes(label = ..count..)) +
  theme_bw()
```

---


## Ajustar el texto para que no se solape con la línea

```{r, fig.height=5}
ggplot(penguins, aes(sex)) +
  geom_bar(fill = "lightblue", color = "black") +
  scale_x_discrete(limits = c("male", "female")) +
  geom_text(stat = "count", aes(label = ..count..), vjust = 2) +
  theme_bw()
```

---

## Ajuste fuera de la barra

```{r}
ggplot(penguins, aes(sex)) +
  geom_bar(fill = "lightblue", color = "black", alpha = 0.5) +
  scale_x_discrete(limits = c("male", "female")) +
  geom_text(stat = "count", aes(label = ..count..), 
            vjust = -.7) +
  scale_y_continuous(limits = c(0,200), breaks = seq(0,200,25)) +
  theme_bw() +
  theme(text=element_text(family = "Times New Roman", size = 16))

```


---

## Añadiendo coloreado


```{r}
ggplot(penguins, aes(sex)) +
  geom_bar(aes(fill = species), color = "black", alpha = 0.5) +
  scale_x_discrete(limits = c("male", "female")) +
  geom_text(stat = "count", aes(label = ..count..), 
            vjust = -.7) +
  scale_y_continuous(limits = c(0,200), breaks = seq(0,200,25)) +
  scale_fill_brewer(palette = "Dark2") + 
  theme_bw() +
  theme(text=element_text(family = "Times New Roman", size = 11))
```

---

## Mover posición de la leyenda

```{r, fig.height=5}
ggplot(penguins, aes(sex)) +
  geom_bar(aes(fill = species), color = "black", alpha = 0.5) +
  scale_x_discrete(limits = c("male", "female")) +
  geom_text(stat = "count", aes(label = ..count..), 
            vjust = -.7) +
  scale_y_continuous(limits = c(0,200), breaks = seq(0,200,25)) +
  scale_fill_brewer(palette = "Dark2") + 
  theme_bw() +
  theme(text=element_text(family = "Times New Roman", size = 11),
        legend.position = "bottom") # top, bottom, none
```


---

# Una barra para cada categoría

```{r, fig.height=5}
ggplot(penguins, aes(sex)) +
  geom_bar(aes(fill = species), color = "black", alpha = 0.5,
           position = "dodge") +
  scale_x_discrete(limits = c("male", "female")) +
  geom_text(stat = "count", aes(group = species, label = ..count..), 
            position = position_dodge(0.9),
            vjust = 1.5, size = 3, color = "white") +
  scale_y_continuous(limits = c(0,200), breaks = seq(0,200,25)) +
  scale_fill_brewer(palette = "Dark2") + 
  theme_bw() +
  theme(text=element_text(family = "Times New Roman", size = 11),
        legend.position = "bottom") # top, bottom, none

```

---

## Utilizando `geom_bar` para ver la distribución

```{r, fig.height = 5}
ggplot(penguins, aes(island, fill = sex)) +
  geom_bar(position = "fill") +
  scale_fill_brewer(palette = "Paired", na.value = "black") +
  scale_y_continuous(labels = paste0(seq(0,100,25), "%")) +
  labs(x = "Isla",
       y = "Total",
       title = "Porcentaje de pingüinos \nen cada isla",
       fill = "Sexo") +
  theme(plot.title = element_text(hjust = 0.6))
```


---

```{r, fig.height=5}
ggplot(penguins, aes(island, fill = sex)) +
  geom_bar(position = "fill") +
  scale_fill_brewer(palette = "Paired", na.value = "#000000", labels = c("hembra", "macho", "desconocido")) +
  scale_y_continuous(labels = paste0(seq(0,100,25), "%")) +
  labs(x="Isla",y="Porcentaje",title="Porcentaje de ping en cada isla", fill = "Sexo") +
  theme(legend.text = element_text(color = "blue"),
        legend.background = element_rect(fill = "darkgray"),
        legend.key = element_rect(fill = "white", color = NA),
        # Change legend key size and key width
        legend.key.size = unit(1.5, "cm"),
        legend.key.width = unit(0.5,"cm"))
```


---
class: middle, center

# Gráficos dinámicos: `gganimate`

---

```
install.packages(babynames)
library(babynames)
```

```{r, fig.height=4}
don <- babynames %>% 
  filter(name %in% c("Ashley", "Patricia", "Helen")) %>%
  filter(sex=="F")

ggplot(don, aes(year, n, group = name, color = name)) +
  geom_point() +
  geom_line() +
  ggtitle("Nombres de la población") +
  transition_reveal(year)
```

---

```{r, fig.height=5}
p2 <- ggplot(gapminder, aes(gdpPercap, lifeExp,
                      size = pop,
                      color = continent)) +
  geom_point() +
  theme_bw() +
  labs(title = "GDP año: {frame_time}") +
  transition_time(year)
animate(p2, renderer = gifski_renderer())
```


---
class: middle, center

# Gráficos interactivos: `ggplotly`

---

# Diagrama básico para contar cuantos pingüinos hay de cada sexo

```{r, fig.height=5}
pl1 <- ggplot(penguins, aes(sex)) +
  geom_bar()
ggplotly(pl1)
```

---

# Diagrama básico para contar cuantos pingüinos hay de cada sexo

```{r, fig.height=5}
pl1 <- ggplot(penguins, aes(sex)) +
  geom_bar()
ggplotly(pl1)
```

---

```{r, fig.height=5}
pl2 <- ggplot(penguins, aes(x = bill_length_mm, 
                            y = bill_depth_mm)) +
  geom_point(aes(fill = island, 
                 shape = species,
                 color = sex),
             size = 5,
             alpha = .6) +
  geom_text(aes(label = ifelse(sex == "male", "M", "F"))) +
  scale_shape_manual(values = c(21,22,23)) +
  scale_color_manual(values = c("white", "grey", "black")) +
  scale_fill_manual(values = c(Biscoe="red", 
                               Dream="blue", 
                               Torgersen="green")) +
  guides(fill = guide_legend(override.aes = list(color = c(Biscoe="red", 
                                                           Dream="blue", 
                                                           Torgersen="green"))))

pl2
```

---

```{r}
pl2
```


---

```{r, fig.height=5}
ggplotly(pl2)
```

---
class: middle, center

# Tarea

---

# Tarea

Para superar el curso, es necesaria la entrega de la siguiente tarea:

- Elegir un conjunto de datos o propio
  - En este [enlace](https://github.com/rfordatascience/tidytuesday) encontrarás muchos conjuntos de datos de todo tipo que puedes utilizar.
- Crear tres gráficos con al menos tres aesthetics.
- Justificar la creación de estos gráficos. ¿Qué quieren mostrar? ¿Por qué se ha elegido ese tipo de gráfico? ¿Por qué se han elegido esas aesthetics?.

- Entregar:
  - Archivo `.R` con el código generado y archivo de datos en caso de que el script no los descargue de forma remota.
  - Explicación de la motivación tras los gráficos generados.

**DEADLINE**: 31 de Mayo 2021, 23:59.

---



